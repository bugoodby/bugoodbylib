tab = CHR($09)

cnt = LENGTH(PARAM_STR)
PRINT "PARAM_STRÇÃêî: " + cnt

FOR i = 0 TO cnt - 1
	IFB PARAM_STR[i] = "" THEN
		PRINT tab + "param" + i + " : ãÛï∂éö"
	ELSEIF PARAM_STR[i] = NULL THEN
		PRINT tab + "param" + i + " : NULL"
	ELSE
		PRINT tab + "param" + i + " : [" + PARAM_STR[i] + "]"
	ENDIF
NEXT

PRINT ""

