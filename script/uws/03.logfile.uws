//GET_CUR_DIRŽæ“¾

GETTIME()
currenttime = G_TIME_YY4 + G_TIME_MM2 + G_TIME_DD2 + "_" + G_TIME_HH2 + G_TIME_NN2 + G_TIME_SS
logpath = GET_CUR_DIR + "\log_" + currenttime + ".log"

f = FOPEN(logpath, F_WRITE)
if f = -1 then
	print "cannot open log file."
endif

FPUT(f, GET_CUR_DIR)

FCLOSE(f)

