
LOGPRINT(TRUE)
logWID = GETID(GET_LOGPRINT_WIN)
logX = STATUS(logWID, ST_WIDTH)
logY = STATUS(logWID, ST_HEIGHT)
ACW(logWID, G_SCREEN_W-logX, 0)
//CTRLWIN(logWID, MIN)

for i = 0 to 100
	PRINT "‚½‚¾‚¢‚Ü" + i + "‰ñ–Ú"
	sleep(0.1)
next

MSGBOX(logWID)


