//
// カーソル下のコントロールの情報をふきだし表示
//
STOPFORM(TRUE)

While True
	wid = GETID(GET_FROMPOINT_WIN)
	cid = GETID(GET_FROMPOINT_OBJ)
	str = cid + " : " + STATUS(cid, ST_TITLE) + " ("+ STATUS(cid, ST_X) + ", " + STATUS(cid, ST_Y) + ")"
	
	x = G_MOUSE_X;  y = G_MOUSE_Y
	Fukidasi(str, x + 10, y + 10, 3)
	Sleep(0.5)
Wend
